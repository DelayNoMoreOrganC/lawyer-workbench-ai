# 律师工作台AI系统 - 开发团队交接文档

## 📋 文档概述

本文档为新的开发团队提供全面的系统结构概览，帮助快速理解项目架构并继续开发。

### 目标受众
- 新加入的开发团队成员
- 前端开发人员（Next.js + React）
- 后端开发人员（FastAPI + Python）
- 全栈工程师
- DevOps工程师

### 项目基本信息
- **项目名称**: 智能律师工作台 AI Lawyer Workspace
- **GitHub仓库**: https://github.com/DelayNoMoreOrganC/lawyer-workbench-ai
- **当前状态**: 第一阶段核心功能已完成
- **技术栈**: Next.js 16 + FastAPI + SQLite/PostgreSQL

---

## 🏗️ 项目结构概览

### 目录结构
```
lawyer-workbench-ai-main/
├── client_new/              # Next.js前端（当前活跃）
│   ├── src/
│   │   ├── app/            # 页面路由
│   │   ├── components/     # React组件
│   │   ├── lib/            # 工具函数
│   │   ├── types/          # TypeScript类型
│   │   └── contexts/       # React Context
│   ├── package.json        # 前端依赖
│   └── next.config.ts      # Next.js配置
├── server/                 # FastAPI后端
│   ├── app/
│   │   ├── main.py         # 应用入口
│   │   ├── api_v2/         # API路由v2.0
│   │   ├── models/         # 数据库模型
│   │   ├── schemas/        # Pydantic模式
│   │   └── core/           # 核心功能
│   ├── requirements.txt    # Python依赖
│   └── .env               # 环境变量
├── client/                 # 旧版Vue前端（兼容保留）
└── 启动脚本               # 快速启动脚本
```

### 技术栈总览

**前端技术栈**
- **框架**: Next.js 16 (App Router) + React 19
- **UI库**: shadcn/ui + Tailwind CSS 4
- **状态管理**: React Hooks + Context API
- **类型安全**: TypeScript 5
- **图标**: lucide-react
- **日期处理**: date-fns

**后端技术栈**
- **框架**: FastAPI 0.104+
- **数据库**: SQLite (开发) / PostgreSQL (生产)
- **ORM**: SQLAlchemy 2.0
- **认证**: JWT + BCrypt
- **AI集成**: DeepSeek API + Ollama
- **文档处理**: PyPDF2, pdfplumber

---

## 🎯 核心功能模块

### 1. 案件管理模块
- **功能**: 案件全生命周期管理（起草中→待立案→一审→二审→执行→结案）
- **数据表**: `cases_new`
- **API**: `/api/v2/cases/`
- **前端页面**: `/cases`, `/cases/[id]`

### 2. 客户管理模块 (CRM)
- **功能**: 客户档案、沟通记录、冲突检测
- **数据表**: `customers`, `communications`, `customer_case_conflicts`
- **API**: `/api/v2/customers/`
- **前端页面**: `/customers`, `/customers/[id]`

### 3. 财务管理模块
- **功能**: 费用管理、律师费、收款管理、发票生成
- **数据表**: `expenses`, `attorney_fees`, `payments`, `invoices`
- **API**: `/api/v2/finance/`
- **前端组件**: `case-finance-manager.tsx`

### 4. 待办任务模块
- **功能**: 任务管理、日历视图、优先级管理
- **数据表**: `tasks`, `events`
- **API**: `/api/v2/tasks/`, `/api/v2/events/`
- **前端页面**: `/tasks`

### 5. AI助手模块
- **功能**: 法律咨询、文书生成、案件分析、文档识别
- **数据表**: `ai_assist_logs`
- **API**: `/api/v2/ai/chat/`, `/api/v2/ai/document/`
- **前端页面**: `/ai-chat`
- **AI模式**: DeepSeek API 或 Ollama本地模型

### 6. 文档管理模块
- **功能**: 电子卷宗、OCR识别、文档分类
- **数据表**: `dossiers`
- **API**: `/api/v2/dossiers/`
- **前端页面**: `/dossiers`

### 7. 行政OA模块
- **功能**: 用印管理、报销审批、公告通知
- **API**: `/api/v2/oa/`
- **前端页面**: `/oa`

### 8. 系统管理模块
- **功能**: 用户管理、权限控制、系统配置
- **数据表**: `users`
- **API**: `/api/v2/users/`, `/api/v2/auth/`
- **前端页面**: `/admin`

---

## 🗄️ 数据库架构

### 核心数据表（13个）

**用户与权限**
- `users` - 用户基本信息（三级权限：admin/lawyer/assistant）

**案件相关**
- `cases_new` - 案件信息（新模型）
- `customers` - 客户信息
- `customer_case_conflicts` - 客户冲突检测
- `communications` - 沟通记录

**任务与日程**
- `tasks` - 待办任务
- `events` - 日程事件

**财务管理**
- `expenses` - 费用支出
- `attorney_fees` - 律师费
- `payments` - 收款记录
- `invoices` - 发票

**文档与AI**
- `dossiers` - 电子卷宗
- `ai_assist_logs` - AI助手日志

### 数据库配置
```python
# 开发环境
DATABASE_URL=sqlite:///./lawyer_workbench_new.db

# 生产环境
DATABASE_URL=postgresql://user:password@localhost/lawyer_db
```

---

## 🌐 API架构

### API版本管理
- **v2.0 API** (`/api/v2/`) - 当前版本，新功能开发
- **v1.0 API** (`/api/`) - 兼容版本，向后兼容

### 主要API端点

**认证相关**
- `POST /api/v2/auth/register` - 用户注册
- `POST /api/v2/auth/login` - 用户登录
- `GET /api/v2/auth/me` - 获取当前用户
- `PUT /api/v2/auth/me` - 更新用户信息

**案件管理**
- `GET /api/v2/cases/` - 获取案件列表
- `POST /api/v2/cases/` - 创建案件
- `GET /api/v2/cases/{id}` - 获取案件详情
- `PUT /api/v2/cases/{id}` - 更新案件
- `DELETE /api/v2/cases/{id}` - 删除案件

**客户管理**
- `GET /api/v2/customers/` - 获取客户列表
- `POST /api/v2/customers/` - 创建客户
- `GET /api/v2/customers/{id}/conflict-check` - 冲突检测

**财务管理**
- `GET /api/v2/finance/expenses` - 获取费用列表
- `POST /api/v2/finance/expenses` - 创建费用记录
- `GET /api/v2/finance/cases/{case_id}/summary` - 案件财务汇总

**AI服务**
- `POST /api/v2/ai/chat/chat` - AI对话
- `POST /api/v2/ai/document/extract-summons` - 文档识别

### API文档
- **Swagger UI**: http://localhost:5000/docs
- **ReDoc**: http://localhost:5000/redoc

---

## 🚀 快速开始

### 环境要求
- **Node.js**: 18+
- **Python**: 3.9+
- **数据库**: SQLite (自带) 或 PostgreSQL

### 一键启动
```bash
# 使用启动脚本
./start.sh

# 或快速启动脚本
./quick-start.sh
```

### 手动启动

**1. 启动后端**
```bash
cd server
pip install -r requirements.txt
python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 5000
```

**2. 启动前端**
```bash
cd client_new
npm install
npm run dev
```

**3. 访问应用**
- 前端: http://localhost:3000
- 后端API: http://localhost:5000
- API文档: http://localhost:5000/docs

### 默认账户
- **用户名**: admin
- **密码**: admin123

---

## 👥 用户权限系统

### 三级权限控制

**管理员 (admin)**
- 所有功能访问权限
- 用户管理权限
- 系统配置权限

**律师 (lawyer)**
- 案件管理权限
- 客户管理权限
- 财务查看权限

**助理 (assistant)**
- 基础案件查看权限
- 任务管理权限
- 文档查看权限

### 权限实现
```python
# 后端权限装饰器
@router.get("/admin-only")
async def admin_function(current_user: User = Depends(require_admin)):
    pass

# 前端权限检查
{user?.role === 'admin' && <AdminComponent />}
```

---

## 🔐 安全架构

### 认证机制
- **JWT Token**: 无状态认证
- **Token有效期**: 30分钟（可配置）
- **密码加密**: BCrypt哈希算法
- **Token存储**: localStorage（前端）

### 安全特性
- **SQL注入防护**: ORM参数化查询
- **XSS防护**: React自动转义
- **CSRF防护**: SameSite Cookie + CORS配置
- **数据验证**: Pydantic模型验证

---

## 🎨 前端架构

### Next.js 16 App Router
- **文件系统路由**: 基于文件结构的自动路由
- **服务端组件**: 默认服务端渲染，提升性能
- **客户端组件**: 使用'use client'指令
- **布局系统**: 嵌套布局复用

### 组件组织
```
components/
├── ui/                 # shadcn/ui基础组件
│   ├── button.tsx
│   ├── card.tsx
│   └── ...
├── navigation.tsx      # 导航栏
├── document-upload.tsx # 文件上传
└── month-calendar-view.tsx # 月历视图
```

### 状态管理策略
- **本地状态**: useState/useReducer
- **全局状态**: React Context
- **服务器状态**: SWR/React Query
- **表单状态**: 受控组件

### UI设计系统
- **组件库**: shadcn/ui + Radix UI
- **样式**: Tailwind CSS 4
- **设计原则**: Apple简笔风格，最小化emoji使用
- **响应式**: 移动优先设计

---

## ⚙️ 后端架构

### FastAPI应用结构
```
app/
├── main.py             # 应用入口，路由注册
├── config.py           # 配置管理
├── api_v2/             # API路由v2.0
│   ├── auth.py         # 认证API
│   ├── cases.py        # 案件API
│   ├── tasks.py        # 任务API
│   ├── customers.py    # 客户API
│   ├── finance.py      # 财务API
│   └── ...
├── models/             # SQLAlchemy数据模型
├── schemas/            # Pydantic验证模式
├── core/               # 核心功能
│   ├── database.py     # 数据库连接
│   └── security.py     # 安全认证
└── utils/              # 工具函数
```

### 业务逻辑分层
```
API层 (api_v2/*.py)
    ↓
业务逻辑层 (services/*.py) - 待完善
    ↓
数据访问层 (models/*.py)
    ↓
数据库
```

### 异步任务
- **任务队列**: Celery + Redis
- **适用场景**: 文档处理、AI分析、邮件发送
- **定时任务**: 任务提醒、数据统计

---

## 📊 开发统计数据

### 当前完成状态
- **开发阶段**: 第一阶段核心功能已完成
- **代码规模**: 66个文件，9,995行代码
- **数据库**: 13个核心表
- **API接口**: 93个接口
- **前端页面**: 15+个页面
- **组件数量**: 30+个组件

### GitHub状态
- **仓库**: https://github.com/DelayNoMoreOrganC/lawyer-workbench-ai
- **分支**: main, feature/user-authentication
- **最新提交**: feat: 完成律师工作台系统第一阶段核心功能优化

---

## 🛠️ 开发指南

### 代码规范

**前端规范**
- **TypeScript**: 使用类型，避免any
- **组件命名**: PascalCase
- **代码格式化**: Prettier
- **ESLint**: 遵循规则
- **性能优化**: React.memo, useMemo, useCallback

**后端规范**
- **PEP 8**: Python代码规范
- **类型提示**: 使用类型注解
- **文档字符串**: 添加docstring
- **错误处理**: 统一异常处理
- **日志记录**: logging模块

### Git工作流

**分支策略**
- `main` - 主分支（生产环境）
- `develop` - 开发分支
- `feature/*` - 功能分支
- `bugfix/*` - Bug修复分支

**提交信息规范**
```
feat: 新功能
fix: Bug修复
docs: 文档更新
style: 代码格式调整
refactor: 重构
test: 测试相关
chore: 构建/工具相关
```

### 开发流程

1. **创建功能分支**
```bash
git checkout develop
git checkout -b feature/your-feature-name
```

2. **开发并提交**
```bash
git add .
git commit -m "feat(scope): description"
```

3. **推送并创建PR**
```bash
git push origin feature/your-feature-name
```

---

## 🧪 测试策略

### 前端测试
- **单元测试**: Jest + React Testing Library
- **组件测试**: 测试组件渲染和交互
- **E2E测试**: Playwright测试用户流程

### 后端测试
- **单元测试**: pytest测试函数和类
- **API测试**: pytest测试API端点
- **数据库测试**: 使用测试数据库

### 测试命令
```bash
# 前端测试
cd client_new
npm test

# 后端测试
cd server
pytest
```

---

## 📦 部署指南

### 环境变量配置

**后端环境变量**
```bash
# 数据库
DATABASE_URL=postgresql://user:password@localhost/lawyer_db

# JWT配置
SECRET_KEY=your-secret-key
ACCESS_TOKEN_EXPIRE_MINUTES=30

# AI配置
AI_MODE=ollama  # deepseek or ollama
DEEPSEEK_API_KEY=your-api-key
OLLAMA_BASE_URL=http://localhost:11434

# 文件配置
UPLOAD_DIR=./uploads
MAX_UPLOAD_SIZE=10485760
```

**前端环境变量**
```bash
NEXT_PUBLIC_API_URL=http://localhost:5000
```

### Docker部署

**前端Dockerfile**
```dockerfile
FROM node:18-alpine
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
RUN npm run build
CMD ["npm", "start"]
```

**后端Dockerfile**
```dockerfile
FROM python:3.9-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt
COPY . .
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "5000"]
```

**Docker Compose**
```yaml
version: '3.8'
services:
  frontend:
    build: ./client_new
    ports:
      - "3000:3000"
  backend:
    build: ./server
    ports:
      - "5000:5000"
    depends_on:
      - db
  db:
    image: postgres:14
```

### 生产环境部署

**前端部署（Vercel）**
```bash
cd client_new
npm run build
vercel --prod
```

**后端部署（Docker）**
```bash
docker build -t lawyer-workbench-api ./server
docker run -d -p 5000:5000 lawyer-workbench-api
```

---

## 🎯 下一步开发计划

### 第二阶段功能
- **AI模型优化**: 更精准的法律AI模型
- **移动端适配**: 响应式设计优化
- **权限系统完善**: 更细粒度的权限控制
- **报表导出**: Excel/PDF报表生成
- **消息通知**: 实时消息推送

### 性能优化
- **前端优化**: 代码分割、懒加载、缓存优化
- **后端优化**: 数据库查询优化、Redis缓存
- **API优化**: 接口响应时间优化

### 功能增强
- **协作功能**: 多用户实时协作
- **数据分析**: 更深入的数据分析功能
- **自动化**: 更多自动化工作流程

---

## 📞 联系与支持

### 技术支持
- **GitHub Issues**: https://github.com/DelayNoMoreOrganC/lawyer-workbench-ai/issues
- **文档**: 查看项目README.md和代码注释

### 常见问题

**Q: 如何添加新的API接口？**
A: 在`server/app/api_v2/`创建新文件，实现路由，然后在`main.py`注册。

**Q: 如何修改前端页面？**
A: 在`client_new/src/app/`找到对应页面文件，修改组件。

**Q: 数据库模型变更怎么办？**
A: 修改`models/`中的模型，创建数据库迁移。

**Q: 如何调试API？**
A: 访问http://localhost:5000/docs使用Swagger UI测试API。

**Q: 如何添加新的UI组件？**
A: 使用shadcn/ui CLI：`npx shadcn@latest add [component]`。

---

## 📚 相关文档

- **项目README**: `/README.md`
- **API文档**: http://localhost:5000/docs
- **开发指南**: 本文档
- **代码注释**: 源码中的详细注释

---

## 🎓 学习资源

### 前端技术
- [Next.js文档](https://nextjs.org/docs)
- [React文档](https://react.dev)
- [shadcn/ui组件](https://ui.shadcn.com)
- [Tailwind CSS](https://tailwindcss.com)

### 后端技术
- [FastAPI文档](https://fastapi.tiangolo.com)
- [SQLAlchemy文档](https://docs.sqlalchemy.org)
- [Pydantic文档](https://docs.pydantic.dev)

### AI技术
- [DeepSeek API](https://platform.deepseek.com)
- [Ollama文档](https://ollama.ai/docs)

---

**文档版本**: v1.0
**最后更新**: 2025-01-09
**维护团队**: 律师工作台开发团队

欢迎新的开发团队加入！如有任何问题，请随时联系我们。
